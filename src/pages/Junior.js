import React from "react";

export const Junior = () => {
  const container = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
  };

  return (
    <div style={container}>
      <h2>Junior</h2>
    </div>
  );
};
