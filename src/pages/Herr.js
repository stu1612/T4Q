import React from "react";

export const Herr = () => {
  const container = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    backgroundColor: "green",
  };

  return (
    <div style={container}>
      <h2>Herr</h2>
    </div>
  );
};
